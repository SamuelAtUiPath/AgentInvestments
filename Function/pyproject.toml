[project]
name = "Function"
description = "A UiPath Function that uploads a local file to a storage bucket"
authors = [{ name = "UiPath", email = "contact@uipath.com" }]
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["uipath>=1.0.0", "pandas>=2.2.0"]
